spring.application.name=portfolio-backend

# PostgreSQL database configuration
#spring.datasource.url=jdbc:postgresql://localhost:5432/portfolio_DB?autoReconnect=true&createDatabaseIfNotExist=true&useSSL=false
spring.datasource.url=jdbc:postgresql://localhost:5432/dev_database?autoReconnect=true&createDatabaseIfNotExist=true&useSSL=false
spring.datasource.username=dev_user
spring.datasource.password=dev_password
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate configurations (optional, but commonly used)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true


#docker run --name dev-database -e POSTGRES_PASSWORD=dev_password -e POSTGRES_USER=dev_user -e POSTGRES_DB=dev_database -p 5432:5432 -v postgres-data:/var/lib/postgresql/data -d postgres